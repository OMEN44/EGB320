[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python>=4.12.0.88",
    "pyfakewebcam",
]

[tool.uv.sources]
pyfakewebcam = { git = "https://github.com/jremmons/pyfakewebcam" }
